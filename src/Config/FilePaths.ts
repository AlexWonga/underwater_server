import path from "path";

export const filesDir = 'deviceFiles';//存放设备文件目录名
export const devicePicturesDir = 'devicePicture';//设备图片存放目录名
export const rootDirPath = path.resolve(__dirname,'..','..');//获得项目目录
export const deviceAttachmentDir = 'deviceAttachment';//设备附件存放文件目录名

